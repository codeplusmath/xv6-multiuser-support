#include "types.h"
#include "stat.h"
#include "user.h"
#include "fs.h"
#include "ls.h"

void integer_to_binary(unsigned in i, int co, int * o);
void print_perm(int mode, int dir);
void lsl(char * p);
